Use the quick workflow for a single Xiaohongshu note.

- Ensure a run exists with `xhs-spec quick`.
- Read brand, strategy, note spec, and current quick artifacts.
- Write back to `brief.md` and `draft.md`.
- Use `<placeholder>...</placeholder>` for any intentionally unresolved field.
- Recommend `xhs-spec review --target <id>` when done.
- If review is blocked, go back and finish `brief.md` or `draft.md` instead of restarting the quick run.
