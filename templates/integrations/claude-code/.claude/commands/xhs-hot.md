Use the trend workflow for a hot topic or fit-check request.

- Ensure a run exists with `xhs-spec hot`.
- Read trend spec, taboo rules, and trend artifacts.
- Write `fit-check.md`.
- Use `<placeholder>...</placeholder>` for any intentionally unresolved field.
- Recommend `xhs-spec fit --target <id> --verdict approved|rejected`.
