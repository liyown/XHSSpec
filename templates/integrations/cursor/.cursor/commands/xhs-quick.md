Create or continue a quick Xiaohongshu note workflow in this repo.

1. Ensure a quick run exists with `xhs-spec quick --idea "<user idea>"` if needed.
2. Read `.xhsspec/brand/*`, `.xhsspec/strategy/*`, `.xhsspec/specs/note.spec.md`, and the target run's `brief.md`.
3. Update `brief.md` and `draft.md`.
4. Use `<placeholder>...</placeholder>` for any intentionally unresolved field.
5. End by suggesting `/xhs:review` or `xhs-spec review --target <id>`.
