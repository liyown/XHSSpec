Create or continue a quick Xiaohongshu note workflow in this repo.

1. Ensure a quick run exists with `xhsops quick --idea "<user idea>"` if needed.
2. Read `.xhsops/brand/*`, `.xhsops/strategy/*`, `.xhsops/specs/note.spec.md`, and the target run's `brief.md`.
3. Update `brief.md` and `draft.md`.
4. Use `<placeholder>...</placeholder>` for any intentionally unresolved field.
5. End by suggesting `/xhs:review` or `xhsops review --target <id>`.
